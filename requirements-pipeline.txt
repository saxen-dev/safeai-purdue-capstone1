# Modular medical pipeline (pipeline/, run_pipeline.py, scripts/*_report.py)
numpy>=1.20,<2
pandas>=1.3
pymupdf>=1.23
rank-bm25>=0.2.2
rapidfuzz>=3.0
tabulate>=0.9.0
pdfplumber>=0.10.0
# Dense retrieval (pipeline/retriever.py — merged from requirements-rag.txt)
sentence-transformers>=2.2.0
chromadb>=0.4.0
faiss-cpu>=1.7.0
# Optional: Docling + TableFormer ACCURATE (CT Health AI integration)
# Replaces PyMuPDF find_tables() with transformer-based table extraction.
# Required when ExtractionConfig.use_docling_tables=True (default).
# Install with: pip install 'docling>=2.64.0'
# docling>=2.64.0

# Optional: ColPali v1.2 visual retrieval (CT Health AI integration)
# Adds a fourth retrieval tier: page-level MaxSim scoring over patch embeddings.
# Catches dosing tables and figures rendered as images (invisible to BM25/FAISS).
# Required for pipeline/colpali_retriever.py and scripts/build_colpali_index.py.
# Install with: pip install 'colpali-engine>=0.3.8' torch torchvision pypdfium2
# After installing, build the index: python scripts/build_colpali_index.py --pdf ... --kb ...
# colpali-engine>=0.3.8
# torch
# torchvision
# pypdfium2
